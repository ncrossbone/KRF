Ext.define('KRF.view.common.DataGrid', {
	extend : 'Ext.grid.Panel',
	       
	xtype : 'cmm-datagrid',
	//store: 'GridStoreTest',
		
	//autoScroll : true,
	
	//layout: 'fit',
	//columns: this.columns,
	
	/*
	initComponent : function() {
		this.tbar = [];
		//alert(this.columns.length);
        for (var i = 0; i < this.columns.length; i++) {
        	this.tbar.push({
                text: this.columns[i].text,
                enableToggle: true,
                pressed: true,
                scope: this,
                toggleHandler: this.onButtonToggle
            });
        }
        
        this.callParent();
	}
	*/
});